<%= form_errors(@settings) %>

<%= form_for @settings do |f| %>
  <%= form_text_field f, :name %>
  <%= form_email_field f, :email %>
  <%= form_date_field f, :deadline, "deadline", datepicker_time(f.object.deadline) %>

  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
