<%
  barcode = Barcodes.create('EAN13', data: data, bar_width: 35, bar_height: 1500, caption_height: 300, caption_size: 275 ) # required: height > size
  rendered = Barcodes::Renderer::Image.new(barcode).render
%>
<img src="data:image/png;base64,<%= Base64.encode64 rendered %>" />
