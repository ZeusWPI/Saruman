<%= link_to raw('<span class="glyphicon glyphicon-edit"></span> Edit'), edit_partner_path(partner), class: "btn btn-default btn-xs" %>
<%= link_to raw('<i class="glyphicon glyphicon-retweet"></i> Resend login link'), partner_resend_path(partner), class: 'btn btn-xs btn-default', remote: true %>
<%= link_to raw('<span class="glyphicon glyphicon-trash"></span> Delete'), "#", class: "btn btn-danger btn-xs delete-confirm", data: { link: partner_path(partner), toggle: 'modal',  target: "#delete-confirm" } %>
