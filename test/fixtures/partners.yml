vtk:
  name: VTK
  barcode_data: <%= 12.times.map { SecureRandom.random_number(10) }.join %>
  barcode: <%= 13.times.map { SecureRandom.random_number(10) }.join %>

hilok:
  name: HILOK
  barcode_data: <%= 12.times.map { SecureRandom.random_number(10) }.join %>
  barcode: <%= 13.times.map { SecureRandom.random_number(10) }.join %>

vlak:
  name: VLAK
  barcode_data: <%= 12.times.map { SecureRandom.random_number(10) }.join %>
  barcode: <%= 13.times.map { SecureRandom.random_number(10) }.join %>

# == Schema Information
#
# Table name: partners
#
#  id           :bigint           not null, primary key
#  barcode      :string           not null
#  barcode_data :string           not null
#  name         :string           not null
#  created_at   :datetime         not null
#  updated_at   :datetime         not null
#
# Indexes
#
#  index_partners_on_name  (name) UNIQUE
#
